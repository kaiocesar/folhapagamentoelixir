version: '3.5'
services:
        db:
                image: postgres

        web:
                build: .
                volumes: 
                        - type: bind
                          source: .
                          target: /app
                ports:
                        - "4000:4000"
                depends_on:
                        - db
                command:
                        - ./run.sh
